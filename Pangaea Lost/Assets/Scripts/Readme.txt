Add the scripts to the character

If a collision layer is not created, then create a collision layer
Once a collision layer is created set the Collision Mask variable to the collision layer

TestFart;
